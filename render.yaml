services:
  - type: web
    name: life-companion
    runtime: docker
    plan: free
    dockerfilePath: Dockerfile
    envVars:
      - key: PORT
        value: 10000
      - key: GEMINI_API_KEY
        sync: false
